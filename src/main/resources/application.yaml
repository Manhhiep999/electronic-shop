spring:
  datasource:
    url: jdbc:mysql://localhost:3306/electronicsshop
    username: root
    password: manhhiep123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  spring:
    jpa:
      hibernate:
        ddl-auto: none